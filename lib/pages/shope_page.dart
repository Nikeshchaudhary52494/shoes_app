import 'package:flutter/material.dart';

class ShopePgae extends StatelessWidget {
  const ShopePgae({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("this is shope page"),
    );
  }
}
